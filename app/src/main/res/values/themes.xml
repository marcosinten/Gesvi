<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Tema mínimo del Activity — solo sirve para la ventana antes de que
        Compose tome el control. El theming real vive en AppTheme.kt.
    -->
    <style name="Theme.AppDeViajes" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
