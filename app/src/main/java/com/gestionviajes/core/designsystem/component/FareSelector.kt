package com.gestionviajes.core.designsystem.component

import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier

/**
 * Componente para seleccionar si el usuario pagará tarifa completa o reducida.
 * TODO: Implementar diseño final (toggle o selector).
 */
@Composable
fun FareSelector(modifier: Modifier = Modifier) {
    Text(text = "Selector de Tarifa (Stub)", modifier = modifier)
}
